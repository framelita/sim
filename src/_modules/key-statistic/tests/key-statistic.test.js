'use strict';

import KeyStatistic from '../key-statistic';

describe('KeyStatistic View', function() {

  beforeEach(() => {
    this.keyStatistic = new KeyStatistic();
  });

  it('Should run a few assertions', () => {
    expect(this.keyStatistic);
  });

});
