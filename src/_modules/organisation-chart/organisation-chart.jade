mixin organisation-chart(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  section#organisation-chart.organisation-chart.pt-50.pb-50(class=spec.__class)&attributes(attributes)
    .container
      h2.title.title--gold Organisation Chart
      .organisation-chart__image
        img(src="../images/org-chart.jpg")
