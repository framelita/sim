'use strict';

import Dare2bBanner from '../dare2b-banner';

describe('Dare2bBanner View', function() {

  beforeEach(() => {
    this.dare2bBanner = new Dare2bBanner();
  });

  it('Should run a few assertions', () => {
    expect(this.dare2bBanner);
  });

});
