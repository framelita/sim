'use strict';

import AboutAccordion from '../about-accordion';

describe('AboutAccordion View', function() {

  beforeEach(() => {
    this.aboutAccordion = new AboutAccordion();
  });

  it('Should run a few assertions', () => {
    expect(this.aboutAccordion);
  });

});
